{
  "name": "telegram-faq-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "cheerio": "1.2.0"
  },
  "scripts": {
    "dev": "node --watch api/telegram.js",
    "check": "node --check api/telegram.js && node --check scripts/crawl-faq.mjs && node --check scripts/validate-knowledge.mjs"
  }
}
